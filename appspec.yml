version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user
permissions:
  - object: "/home/ec2-user"
    owner: ec2-user
    group: ec2-user
    mode : 777
hooks:
  AfterInstall:
    - location: scripts/install_dependencies
      runas: ec2-user


